<% if object.errors.any? %>
<!-- NOTE: notice the class applied to this div - we can add custom CSS later for it this way -->
<div class>
  <h2><%= pluralize(object.errors.count, "error") %> prevented this record from being saved:</h2>
  <ul>
  <% object.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
